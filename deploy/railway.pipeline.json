{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m nflfp.pipeline refresh",
    "cronSchedule": "0 12 * * 2",
    "restartPolicyType": "NEVER"
  }
}
