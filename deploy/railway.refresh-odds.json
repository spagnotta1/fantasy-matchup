{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m nflfp.jobs run refresh_odds",
    "cronSchedule": "15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
