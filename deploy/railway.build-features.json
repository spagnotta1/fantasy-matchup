{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m nflfp.jobs run build_features",
    "cronSchedule": "0 13 * * 2",
    "restartPolicyType": "NEVER"
  }
}
