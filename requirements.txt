# Kept in sync with pyproject.toml [project.dependencies].
# Prefer `pip install -e .` — this file exists for tooling that expects it.
duckdb>=1.1
psycopg[binary]>=3.1
sqlalchemy>=2.0.30
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.2
fastapi>=0.115
uvicorn[standard]>=0.30
redis>=5.0
httpx>=0.27
